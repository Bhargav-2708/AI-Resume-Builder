{
  "version": 2,
  "builds": [
    {
      "src": "backend/server.ts",
      "use": "@vercel/node"
    },
    {
      "src": "frontend/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],
  "rewrites": [
    { "source": "/api/(.*)", "destination": "backend/server.ts" },
    { "source": "/(.*)", "destination": "frontend/dist/$1" },
    { "source": "/(.*)", "destination": "frontend/dist/index.html" }
  ]
}
